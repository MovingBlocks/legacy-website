{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "preview",
      "type": "shell",
      "command": "jekyll serve --watch -P 4000 --force_polling -H 0.0.0.0 --drafts"
    }
  ]
}